DATABASE_URL="postgresql://user:password@localhost:5432/stylehub?schema=public"
JWT_SECRET="cambiame_a_un_valor_seguro"
PORT=5000
``` ````

2) Asegurar que app.ts monte Swagger UI — reemplaza/pegalo en:
````ts
// filepath: 
import express from 'express';
import cors from 'cors';
import helmet from 'helmet';
import dotenv from 'dotenv';
import routes from './routes/index';
import swaggerUi from 'swagger-ui-express';
import swaggerSpec from '../swagger.json';
import { errorHandler } from './middleware/errorHandler';

dotenv.config();

const app = express();

app.use(helmet());
app.use(cors());
app.use(express.json());

// Sirve Swagger UI en /api/docs
app.use('/api/docs', swaggerUi.serve, swaggerUi.setup(swaggerSpec));

// Rutas de la API
app.use('/api', routes);

// Error handler global
app.use(errorHandler);

const PORT = process.env.PORT || 5000;
app.listen(PORT, () => {
  console.log(`StyleHub backend listening on port ${PORT}`);
});
``` ````

3) README corto con comandos Windows (crealo/actualiza)
````md
// filepath: 
# StyleHub - Instrucciones rápidas

Backend:
1. Abrir terminal en: C:\Universidad\PF-WEB1\Proyecto_Final\backend
2. Copiar .env: copy .env.example .env  (editar valores)
3. npm install
4. npx prisma generate
5. npx prisma migrate dev --name init
6. npm run seed
7. npm run dev
8. Swagger UI: http://localhost:5000/api/docs

Frontend:
1. Abrir terminal en: C:\Universidad\PF-WEB1\Proyecto_Final\frontend
2. npm install
3. npm run dev
4. Abrir: http://localhost:3000
``` ````

Qué verificar/después de pegar:
- Abre backend en PowerShell:
  cd C:\Universidad\PF-WEB1\Proyecto_Final\backend
  copy .env.example .env
  npm install
  npx prisma generate
  npx prisma migrate dev --name init
  npm run seed
  npm run dev
- Abre frontend:
  cd C:\Universidad\PF-WEB1\Proyecto_Final\frontend
  npm install
  npm run dev

Comprueba:
- http://localhost:5000/api/docs → Swagger UI con tu swagger.json
- Endpoints: GET /api/products, POST /api/auth/register, POST /api/auth/login, POST /api/cart, POST /api/orders, etc. (deben sumar ≥10)

Si al levantar algo falla, copia aquí el error exacto y lo arreglo. ¿Quieres que también agregue protección de rutas en el frontend (guardas) y esquema adicional en swagger.json (más detalles para todos los endpoints)?